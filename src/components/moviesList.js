import React, { useState } from "react";
import { Box } from "@mui/material";

const MoviesList = () => {
  return (
      <Box sx={{ padding: "2em" , border : '1px solid red'}}>
      </Box>
  );
};

export default MoviesList;
